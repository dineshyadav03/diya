# --- Secrets / local TLS ---
# Local TLS certificates (configured with DIYA_SSL_CERT/DIYA_SSL_KEY or auto-discovered)
*.pem
*.key
.env
.env.*

# --- Personal data and runtime state (Diya creates these as it runs) ---
diya.db
backups/
user_profile.txt
dream_state.json
dream_log.txt
dream_pending.jsonl
watcher_state.json
watcher_log.txt
proactive_context.txt

# --- Dependencies and build output ---
node_modules/
.next/
__pycache__/
*.pyc

# --- Editor / OS / tooling ---
.DS_Store
.claude/settings.local.json

# --- Test / tool caches ---
.pytest_cache/
.ruff_cache/

# --- Personal working notes (kept locally, never published) ---
CLAUDE.md
